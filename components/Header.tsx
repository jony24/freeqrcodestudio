'use client';import Link from 'next/link';import {Menu,X,Sun,Moon} from 'lucide-react';import {useState} from 'react';import {nav} from '@/config/site';
export default function Header(){const[open,setOpen]=useState(false);const[dark,setDark]=useState(false);function toggle(){document.documentElement.classList.toggle('dark');setDark(!dark)}return <header className="sticky top-0 z-40 border-b bg-white/95 backdrop-blur dark:bg-slate-950/95"><div className="container flex h-16 items-center justify-between gap-4"><Link href="/" className="flex items-center gap-2 font-bold text-slate-950 dark:text-white"><img src="/logo.svg" alt="Free QR Code Studio" className="h-9 w-auto"/></Link><nav className="hidden items-center gap-6 md:flex">{nav.map(([n,h])=><Link key={h} href={h} className="focus-ring text-sm font-medium text-slate-600 hover:text-brand dark:text-slate-300">{n}</Link>)} </nav><div className="flex items-center gap-2"><label className="sr-only" htmlFor="language">Language</label><select id="language" aria-label="Language" className="hidden rounded-lg border bg-transparent px-2 py-1 text-xs md:block" defaultValue="EN"><option value="EN">EN</option></select><button aria-label="Toggle dark mode" onClick={toggle} className="focus-ring rounded-lg p-2 hover:bg-slate-100 dark:hover:bg-slate-800">{dark?<Sun size={18}/>:<Moon size={18}/>}</button><Link href="/qr-code-generator" className="hidden rounded-xl bg-brand px-4 py-2 text-sm font-semibold text-white md:block">Create QR Code Free</Link><button aria-label="Open menu" onClick={()=>setOpen(!open)} className="focus-ring rounded-lg p-2 md:hidden">{open?<X/>:<Menu/>}</button></div></div>{open&&<nav className="border-t bg-white p-4 md:hidden dark:bg-slate-950">{nav.map(([n,h])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="block rounded-lg px-3 py-3 font-medium">{n}</Link>)}<Link href="/qr-code-generator" onClick={()=>setOpen(false)} className="mt-2 block rounded-xl bg-brand px-4 py-3 text-center font-semibold text-white">Create QR Code Free</Link></nav>}</header>}
