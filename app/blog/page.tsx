import Link from 'next/link';import {posts} from '@/lib/blog';export const metadata={title:'QR Code Blog & Guides | Free QR Code Studio',description:'Practical guides about creating, printing, testing, and using QR codes for websites, WiFi, businesses, restaurants, PDFs, YouTube, and social media.'};export default function Page(){return <main className="container py-16"><div className="mx-auto max-w-3xl text-center"><h1 className="text-4xl font-black">QR Code Blog & Guides</h1><p className="mt-4 text-lg text-slate-600">Practical information for creating and using QR codes effectively.</p></div><div className="mt-12 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{posts.map(p=><Link href={'/blog/'+p.slug} key={p.slug} className="rounded-2xl border p-6 hover:shadow-soft"><div className="text-xs font-semibold uppercase tracking-wider text-brand">{p.category} · {p.reading}</div><h2 className="mt-3 text-xl font-bold">{p.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{p.excerpt}</p><div className="mt-5 text-sm font-semibold text-brand">Read guide →</div></Link>)}</div></main>}
