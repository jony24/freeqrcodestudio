import {notFound} from 'next/navigation';import {tools} from '@/lib/tools';import QrGenerator from '@/components/QrGenerator';import Link from 'next/link';
export function generateStaticParams(){return tools.map(t=>({tool:t.slug}))}
export async function generateMetadata({params}:{params:Promise<{tool:string}>}){const {tool}=await params;const t=tools.find(x=>x.slug===tool);return t?{title:t.title,description:t.description}:{}};
const extras:Record<string,string>={
'url-to-qr-code':'A URL QR code lets someone open a website by scanning a printed or on-screen code. It is useful for landing pages, booking pages, menus, product information, campaigns, and business cards.','text-to-qr-code':'Text QR codes are useful when the information is short and you want to make it available without requiring typing.','email-qr-code':'Email QR codes can prefill a recipient, subject, and message in the user’s mail application.','phone-number-qr-code':'Phone QR codes can make a phone number easier to use from posters, signs, business cards, and service counters.','sms-qr-code':'SMS QR codes can prepare a message for a mobile phone’s messaging app.','wifi-qr-code':'WiFi QR codes can simplify guest access to a wireless network. Keep the printed code private when it contains a network password.','vcard-qr-code':'A vCard QR code packages contact details into a format supported by many mobile devices and contact applications.','location-qr-code':'Location QR codes can open a map coordinate, which is useful for venues, property listings, deliveries, and events.','event-qr-code':'Event QR codes can carry basic event information such as title, time, location, and description.','pdf-qr-code':'A PDF QR code should point to a publicly reachable PDF URL. This version does not upload or host PDF files.','youtube-qr-code':'YouTube QR codes are convenient for posters, classrooms, product displays, event screens, and creator materials.','social-media-qr-code':'Social QR codes can point to a public profile, page, or social landing URL.','payment-qr-code':'Payment QR codes in this tool encode a payment or checkout URL. The QR generator does not process payments.','multi-url-qr-code':'Multi-URL QR generation here encodes the supplied list as text. It does not create a server-side dynamic redirect or device-aware routing service.','business-qr-code':'Business QR codes can connect customers to a website, contact card, map location, social profile, or another public business resource.','restaurant-qr-code':'Restaurant QR codes commonly point to digital menus, ordering pages, reservations, maps, or feedback forms.','whatsapp-qr-code':'WhatsApp QR codes can point to a WhatsApp chat URL so customers can start a conversation without manually entering a number.'};
export default async function Page({params}:{params:Promise<{tool:string}>}){const {tool}=await params;const t=tools.find(x=>x.slug===tool);if(!t)notFound();return <main className="container py-12"><div className="mx-auto max-w-3xl text-center"><h1 className="text-4xl font-black">{t.name} QR Code Generator</h1><p className="mt-4 text-lg text-slate-600">{t.description}</p></div><div className="mt-10"><QrGenerator initialType={t.key}/></div><section className="prose prose-slate mx-auto mt-12"><h2>About {t.name} QR codes</h2><p>{extras[t.slug]}</p><h2>How to create one</h2><ol><li>Enter the requested information in the generator above.</li><li>Review the QR preview and keep strong foreground/background contrast.</li><li>Download the format you need.</li><li>Scan-test the exported code before distribution.</li></ol><h2>Practical tips</h2><ul><li>Keep a clear margin around the QR code.</li><li>Use a destination that will remain available after publication.</li><li>Test printed and on-screen versions at their intended viewing distance.</li></ul><h2>Related QR tools</h2><div className="not-prose grid gap-3 sm:grid-cols-2">{tools.filter(x=>x.slug!==t.slug).slice(0,6).map(x=><Link key={x.slug} href={'/'+x.slug} className="rounded-xl border p-4 font-medium">{x.name} QR Code →</Link>)}</div></section></main>}
